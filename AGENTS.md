# AGENTS.md

本文件约定在此仓库中工作的开发者与自动化 Agent 应遵循的基本规则。

## 项目概况

- 这是一个轻量的原生 Android Emby 客户端。
- 应用模块位于 `app/`，主要代码位于 `app/src/main/java/com/example/embylite/`。
- 当前界面主要使用 Java 动态构建，不要在没有明确收益时引入新的 UI 框架或大型依赖。
- 最低 Android 版本为 API 26，目标和编译版本以 `app/build.gradle` 为准。

## 修改原则

- 先阅读相关代码和 `DEVELOPMENT.md`，确认现有行为后再修改。
- 保持改动聚焦，不顺手重构与当前任务无关的代码。
- 保留现有功能：登录、媒体库、收藏、最近播放、合集、随机播放、外部播放器和删除确认。
- 不修改默认服务器、默认账号或默认密码，除非用户明确要求。
- 不改变 Emby API 请求语义、播放 URL 或凭据存储方式，除非任务本身涉及这些内容。
- 删除媒体、覆盖交付文件或进行其他不可逆操作前，必须确认目标准确且在用户授权范围内。

## 代码风格

- Java 使用 4 空格缩进，类名使用 `UpperCamelCase`，方法和变量使用 `lowerCamelCase`，常量使用 `UPPER_SNAKE_CASE`。
- 优先使用清晰的小方法，避免在单个方法中继续堆积大量重复的视图样式代码。
- 用户可见文本优先放入 `app/src/main/res/values/strings.xml`，避免拼接可翻译文本。
- 共用颜色必须通过 `ThemePalette` 管理，不在页面或适配器中散落硬编码主题色。
- 新增界面必须同时检查深色和浅色模式，保证文字、边框、占位图、按钮和系统栏均有足够对比度。
- 后台网络与图片请求继续使用现有 `ExecutorService`，不得在主线程执行网络访问。
- 更新异步视图时，要确认页面和数据项仍然有效，避免回调更新已经复用或销毁的视图。

## UI 与交互

- 延续简洁、现代、轻量的视觉风格，优先复用现有圆角、间距、按钮和卡片样式。
- 保持手机与平板兼容；列表列数和主要内容宽度应尽量自适应，不写死为单一设备尺寸。
- 重要操作必须有明确反馈；永久删除必须保留二次确认和不可撤销提示。
- 主题切换应即时生效并通过 `SharedPreferences` 保存。
- 不用仅靠颜色表达选中、危险或禁用状态，应同时使用文字、图标或形状差异。

## 构建与验证

- 完成代码修改后至少运行：

  ```bash
  ./gradlew assembleDebug
  ```

- 修改资源、Manifest、主题或界面时，尽量同时运行：

  ```bash
  ./gradlew lintDebug
  ```

- 构建成功后确认 APK 的包名、版本号和文件存在。
- 项目约定的调试交付文件为根目录的 `EmbyLite-debug.apk`。生成新 APK 后，应使用：

  ```bash
  cp -f app/build/outputs/apk/debug/app-debug.apk EmbyLite-debug.apk
  ```

- 覆盖后使用 `sha256sum` 校验根目录 APK 与构建产物一致。
- 若修改会影响用户安装升级，应同步更新 `versionCode` 和 `versionName`，并更新 `DEVELOPMENT.md` 中的当前版本。

## 文档与交付

- 新增功能、改变交互或调整构建方式时，同步更新 `DEVELOPMENT.md`。
- 最终说明应列出主要改动、验证结果以及可直接使用的 APK 路径。
- 不提交临时工具链、下载缓存、日志、密钥、Token 或本机环境配置。

## 安全约定

- 不在日志、文档、截图或回复中暴露用户密码、Token、Keystore 内容或完整认证响应。
- 不把硬编码到 APK 的信息视为秘密；公开发行前必须重新审查默认服务器与凭据配置。
- 网络请求保持合理的连接和读取超时。
- 对服务器返回内容做空值和异常处理，错误提示应可理解，但不要包含敏感认证信息。
